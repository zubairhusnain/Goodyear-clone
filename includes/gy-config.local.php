<?php
declare(strict_types=1);

/**
 * cPanel: site files live in public_html (domain root). Images need an empty base path.
 * If your site is in a subfolder instead, change to e.g. define('GY_BASE_PATH', '/goodyear_offline');
 */
define('GY_BASE_PATH', '');
